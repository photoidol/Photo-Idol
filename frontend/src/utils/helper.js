export const REACT_APP_BACKEND_URL = "https://api.fotoidol.com/api/v1";
export const BACKEND_URL = "https://api.fotoidol.com";

export function isEmptyObject(obj) {
  return Object.keys(obj).length === 0;
}
